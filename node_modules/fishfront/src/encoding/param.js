var param = {
	_emptyData(data){
		return ( data != 0 && data != '' && !data);
	},
	_buildSingleParam(prefix,data){
		if( prefix == '' && data == ''){
			return '';
		}else if( prefix == ''){
			return data;
		}else{
			return encodeURIComponent(prefix)+'='+encodeURIComponent(data);
		}
	},
	_buildParam(prefix,data){
		if( this._emptyData(data) ){
			return this._buildSingleParam(prefix,'');
		}else if( typeof data == 'number' || typeof data == 'string'){
			return this._buildSingleParam(prefix,data);
		}else{
			var result = [];
			for( var i in data ){
				var single = '';
				if( prefix != '' ){
					single += '['
				}
				if( prefix.constructor != Array ){
					single += i;
				}
				if( prefix != '' ){
					single += ']';
				}
				var singleResult = this._buildParam(prefix+single,data[i]) ;
				if( singleResult != ''){
					result.push( singleResult );
				}
			}
			return result.join('&');
		}
	},
	encode(data){
		return this._buildParam('',data);
	}
}
export default param;